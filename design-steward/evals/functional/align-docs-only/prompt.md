---
description: Align must update the design doc to the user's decision and leave source code alone.
max_turns: 30
timeout_seconds: 600
allowed_tools: [Read, Glob, Grep, Skill, Write, Edit]
---

We've decided: buttons use the global 8px radius, including the primary button. Update the project docs to reflect that decision. Don't touch the code yet.
