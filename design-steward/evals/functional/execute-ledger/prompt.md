---
description: Execute must apply the approved change and record it in the run ledger. Needs Bash (WSL2, macOS, Linux or CI).
max_turns: 40
timeout_seconds: 900
allowed_tools: [Read, Glob, Grep, Skill, Write, Edit, Bash]
---

Implement the approved change in brief.md and keep a resumable progress log in case I get interrupted.
