# Fixture standards

Global radius is 8px. Accent is blue #3b82f6. The button currently uses 6px,
which is an unresolved implementation conflict. No user has approved 6px globally.
At narrow viewports the primary button spans the content width.
