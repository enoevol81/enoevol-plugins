---
description: A user-worded request that should load design-steward:audit.
tags: [routing]
max_turns: 4
timeout_seconds: 180
allowed_tools: [Read, Glob, Grep, Skill]
---

Before we redesign the settings page: what design rules is this repo actually committed to for buttons, and where did each rule come from?
