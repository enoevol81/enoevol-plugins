# Example: a scoped button improvement

Request: Implement the approved 6px button radius and check desktop/mobile.

Plan: use the plan JSON in `../../../references/execution.md`. Inspect current
source, change only the approved component/token, then verify both viewports.
Record source and check outputs as evidence. If the preview is unavailable, mark
verification blocked with its exact next action. Resume after the preview is
available. No /goal consumer, special agent roster or elevated permission needed.
